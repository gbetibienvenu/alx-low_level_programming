#include "main.h"

/**
 * _abs - it is an absolute value of integer that can not be change
 *
 * @a: as int
 *
 * Return: always abc letters
 */
int _abs(int a)
{

	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (a * -1);
	}
}
